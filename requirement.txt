python -m venv myenv
pip install langchain-core langchain_community langgraph langchain langchain-huggingface transformers torch
pip install -U duckduckgo-search pip install langchain_groq
pip install ipython
pip install langgraph-supervisor

pip install pypdf

pip install psycopg2-binary pgvector
pip install -U langchain-huggingface
pip install langchain-postgres
pip install psycopg[c]

pip install openai
